import {
}

extern "C" {
    fn printf(str &CStr, val Int) Int;
}

fn fib(n Int) Int {
    if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    }
}

fn main() Int {
    printf(&c"%d\n", fib(43));
    0
}
