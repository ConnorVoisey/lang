import {
}

extern "C" {
    fn printf(str &CStr, param_1 Int, param_2 Int) Int;
}

struct Foo {
    x Int,
    y Int,
}

fn main() Int {
    let foo = Foo {
        x: 5,
        y: 22,
    };

    printf(&c"Foo { x: %d, y: %d }
", foo.x, foo.y);
    0
}
