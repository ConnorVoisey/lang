import {
}

extern "C" {
    fn printf(str &CStr, val Int) Int;
    fn print_foo(foo &Foo) Int;
}

struct Foo {
    num Int,
    y Int,
}

fn main() Int {
    let foo = Foo {
        num: 20,
        y: 55,
    };

    print_foo(&foo);
    0
}
