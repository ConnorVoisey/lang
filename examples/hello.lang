import {
}

extern "C" {
    fn printf(str &CStr, param_1 Int) Int;
}

fn add(num_1 Int, num_2 Int) Int {
    num_1 + num_2
}

fn main() Int {
    let foo = 35;
    let bar = 10;

    if add(foo, bar) < 40 {
        foo = bar + foo;
    };

    printf(&c"foo: %d
", foo);
    0
}
