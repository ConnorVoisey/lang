import {
}

extern "C" {
    fn printf(str &CStr, val Int) Int;
}

struct Foo {
    num Int,
}

struct Bar {
    foo Foo,
    baz &Baz,
}

struct Baz {
    bar Bar,
    foo Foo,
}

fn main() Int {
    let foo = Foo {
        num: 20,
    };

    printf(&c"%d
", foo.num);
    0
}
