import {
}

extern "C" {
    fn printf(str &CStr, val Int) Int;
}

fn main() Int {
    let num = 0;
    let x = 20;

    while num < 10 {
        num = num + 1;
        if (x - num) < 15 {
            break;
        };
    }

    printf(&c"%d
", num);
    0
}
