import {
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(fmt &CStr, param Int) Int;
}

fn main() Int {
    let res = {
        puts(&c"testing");
    } + {
        puts(&c"testing");
    };

printf(&c"res:\t\'%d\"\n", res);
    0
}

