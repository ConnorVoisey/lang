import {
    "fmt/print_ln"
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(str &CStr, val Int) Int;
}

fn add(num1 Int, num2 Int) Int {
    num1 + num2
}

struct Str {
    len Uint,
    chars &CStr
}


fn main() Int {
    add(2, 120);
    puts(&c"hello 2");
    0
}
