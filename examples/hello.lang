import {
    "fmt/print_ln"
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(str &CStr, val Int) Int;
}

struct Str {
    len Uint,
    chars &CStr
}

fn add(num1 Int, num2 Int) Int {
    num1 + num2
}

fn main() Int {
    let foo = 5;
    let bar = add(2, 5);
    printf(&c"hello 1 edited %d
", foo + bar);
    puts(&c"hello 2");
    0
}
