import {
}

extern "C" {
    fn printf(str &CStr, param_1 Int) Int;
}

fn add(num_1 Int) Int {
 num_1
}

fn main() Int {
    let foo = add(15);
    let bar = 10;

    if foo < 20 {
        foo = bar + foo;
    };

    printf(&c"foo: %d
", foo);
    0
}
