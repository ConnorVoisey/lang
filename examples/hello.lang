import {
}

extern "C" {
    fn puts(str &CStr) I32;
    fn printf(fmt &CStr, param I32) I32;
}

// enum Res {
//     Ok,
//     Err,
// }
// 
// struct Pos {
//     x I32,
//     y I32,
// }

fn main() I32 {
    // This is a work in progress
    // let res = Res::Ok;

    let arr = [1];
    arr[0] = 2;

    printf(&c"[%d]\n", arr[0]);

    0
}
