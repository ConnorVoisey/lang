import {
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(fmt &CStr, param Int, param2 Int) Int;
}

enum Res {
    Ok,
    Err,
}

struct Pos {
    x Int,
    y Int,
}

fn main() Int {
    // This is a work in progress
    // let res = Res::Ok;

    let pos = Pos { x: 5, y: 100 };
    printf(&c"pos: { x: %d, y: %d }\n", pos.x, pos.y);

    let arr = [20, 35, 65, 87];
    printf(&c"res: %d, %d\n", arr[0], arr[3]);

    0
}

