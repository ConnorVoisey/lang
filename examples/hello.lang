import {
    "fmt/print_ln"
}

extern "C" {
//    fn puts(str &CStr) Int;
//    fn printf(str &CStr, val Bool) Int;
    fn printf(str &CStr, val Int) Int;
}

struct Str {
    len Uint,
    chars &CStr
}

fn fib(n Int) Int {
    let num = if n < 2 {
        n
    } else {
        fib(n - 1) + fib(n - 2)
    };

    num
}

fn main() Int {
    printf(&c"%d
", fib(3));
    0
}
