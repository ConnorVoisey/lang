import {
    "fmt/print_ln"
}

extern "C" {
//    fn puts(str &CStr) Int;
    fn printf(str &CStr, val Bool) Int;
}

struct Str {
    len Uint,
    chars &CStr
}


fn fib(n Int) Bool {
    5 < 3
}

// fn fib(n Int) Int {
//     if n < 2 {
//         n
//     } else {
//         fib(n - 1) + fib(n - 2)
//     }
// }

fn main() Int {
    printf(&c"%d
", fib(10));
    0
}
