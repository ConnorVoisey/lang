import {
}

extern "C" {
    fn printf(str &CStr, param_1 Int) Int;
}

fn main() Int {
    let foo = 5;
    let bar = 10;
    let arr = [4, 6, 12];

    if foo < 20 {
        foo = bar + foo;
    };

    printf(&c"foo: %d
", foo);
    0
}
