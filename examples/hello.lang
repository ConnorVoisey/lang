import {
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(str &CStr, param_1 Int) Int;
}

fn add() Int {
    20
}

fn main() Int {
    let foo = puts(&c"testing");
    let bar = 10;

    if foo < 20 {
        foo = bar + foo;
    };

    printf(&c"foo: %d
", foo);
    0
}
