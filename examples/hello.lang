import {
}

extern "C" {
    fn puts(str &CStr) Int;
    fn printf(fmt &CStr, param Int, param2 Int) Int;
}

fn main() Int {
    let arr = [20, 35, 65, 87];
    printf(&c"res: %d, %d\n", arr[0], arr[3]);

    0
}

