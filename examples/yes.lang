import {
}

extern "C" {
    fn strlen(str &CStr) I32;
    fn malloc(size I32) [U8; 8196];
    fn write(file I32, buffer [U8; 8196], size I32) I32;
}

fn main() I32 {
    let buffer_size = 8196;
    let buffer = malloc(buffer_size);
    let i = 0;
    while i < buffer_size - 3 {
        buffer[i] = 121; // y
        buffer[i + 1] = 10; // \n
        i = i + 2;
    }
    buffer[buffer_size - 1] = 0; // \0

    while true {
        write(1, buffer, buffer_size);
    }
    
    0
}

